    @InjectMocks
    private ${model_name}ServiceImpl ${lowercase_model_name}Service;
    private final ${class_name} ${lowercase_model_name} = Static${model_name}.${lowercase_model_name}1();
    private final ${class_name} ${lowercase_model_name}2 = Static${model_name}.${lowercase_model_name}2();

    @BeforeEach
    public void setUp() {
        MockitoAnnotations.initMocks(this);
    }

    @Test
    public void testCreate() {
